dp {
    provides fleet_size [Nat]
    provides student_capacity [Nat]
    provides wheelchair_buses [Nat]

    requires capital_cost [USD]

    implemented-by yaml resource("fleet_bus_count.dpc.yaml")
}